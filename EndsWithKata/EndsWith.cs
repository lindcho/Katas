namespace EndsWithKata
{
    public class EndsWith
    {
        public static bool Solution(string str, string ending)
        {
            // TODO: complete
            return str.EndsWith(ending);

        }
    }
}